import React from 'react';
import './style.scss';

const Toolbar = (props) => (
    <div className="toolbar">
        <header>
            <div>Menu</div>
            <div>Logo</div>
            <nav>
                <ul>
                    items
                </ul>
            </nav>
        </header>
       
    </div>
)
export default Toolbar;